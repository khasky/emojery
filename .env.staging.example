# Optional overrides for `pnpm build:staging` - copy to .env.staging (git-ignored).
# The staging build already targets https://api-staging.emojery.app by
# default, so most contributors don't need this file at all. Do NOT commit real
# values to this repo; shell env vars override this file.

# Point the build at a different backend URL.
WXT_API_BASE=
